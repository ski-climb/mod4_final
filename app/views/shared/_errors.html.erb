<% if @errors %>
  <div class="container alert alert-danger">
    <h3><%= pluralize(@errors.count, 'error') %> prevented the user to be created</h3>
    <ul>
      <% @errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
